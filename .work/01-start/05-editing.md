Update the UI to have robust editing features on the artifacts with the ability to see the list of versions of that artfifact, to revert to a previous version of that artifact, see which one was generated vs. which one was edited, etc.
All the UI should be both mobile and desktop friendly.
