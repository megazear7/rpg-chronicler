Massively improve the quality of the user experience, styles, and design.
I do not like the gradient buttons.
I want more round edges, box shadows, etc.
Use color to indicate statuses in the stages of the job.
Format the upload file with some fancy formatting options.
Add a navigation to the bottom of the page so that the user can navigate to the job list page.
Show the most recent jobs on the home page.
Provide a log page to the jobs so that the job processing can add logs for the job that the UI can expose in a pretty way to gain quick insight into what happened, what went wrong, errors, etc.
