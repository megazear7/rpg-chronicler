Create new API services to allow for the various generated artifacts to be viewed and edited.
Editing should be versions, so that if the artifacts (such as the summary, title, or dm notes) are edited, then the original is still saved,each edit is saved, and there are API services for reverting to previous versions, deleting versions, etc.
